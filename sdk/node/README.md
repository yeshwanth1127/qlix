# @qlix/sdk (Node) — TODO

Reserved for the Node/TypeScript port of the Qlix SDK. The Python implementation
in [`../python/`](../python) is the reference; the wire format (canonical JSON
over `signedPayload`, hex Ed25519 signatures, `/api/v1/actions/start` and
`/api/v1/actions/complete`) is shared and stable.

Not built yet — see the Python source for the canonical layer breakdown.
