# Secrets — never commit
**/.env
**/.env.local
**/.env.*.local
qlix-PAT.txt
*.pat
*-PAT.txt
secrets/*
!secrets/.gitignore
*.pem
*.key
youtube-cookies.txt
**/youtube-cookies.txt

# Runtime state (contains agent signing keys)
backend/.qlix-runners/

# Postgres data — bind-mounted so it survives Docker Desktop VM resets
data/postgres/

# Dependencies / build
node_modules/
dist/
.next/
out/
*.tsbuildinfo

# Logs / OS
logs/
*.log
.DS_Store
